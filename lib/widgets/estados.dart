import 'package:flutter/material.dart';

class Estados extends StatelessWidget {
  const Estados({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Estados de Anime"));
  }
}
